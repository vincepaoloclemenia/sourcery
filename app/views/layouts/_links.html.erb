
<ul class="nav navbar-nav navbar-right">
    
    <% if can? :read, Timelog %>
        <li class="<%= current_link?('timelogs', 'index') %>"><%= link_to "Timelogs", timelogs_path, class: "menu-link" %></li>
    <% end %>
    <li class="<%= current_link?('pages', 'notifications') %>"><%= link_to "Notifications", notifications_path, class: 'menu-link' %></li>
    <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><img src="<%= current_user.avatar.url %>" class="avatar-image" height="25px" width="25px"> <span class="caret"></span></a>
        <ul class="dropdown-menu" role="menu">
            <li><%= link_to "Logout", destroy_user_session_path, method: :delete %></li>
        </ul>   
    </li>
   
</ul>
